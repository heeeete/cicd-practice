# Optional GitHub Personal Access Token
# Only needed if you want higher API rate limits (60 req/hour without token, 5000 with token)
# For production, consider using a backend proxy instead of client-side authentication
# VITE_GITHUB_TOKEN=your_github_token_here
